@tailwind base;
@tailwind components;
@tailwind utilities;
@tailwind variants;

body, html {
    @apply h-full;
}

.light body {
    @apply bg-none;
    @apply bg-white;
}

.dark body {
    @apply bg-none;
    @apply bg-black;
}